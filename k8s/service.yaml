apiVersion: v1
kind: Service
metadata:
  name: pokemon-forecaster-api
spec:
  selector:
    app: pokemon-forecaster-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
  type: ClusterIP
